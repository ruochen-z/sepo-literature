# Project

## Overview
- Quartz + Sepo literature-site template: durable content in content/papers/ (per-BibTeX-citekey notes), content/notes/ (synthesis), bibliography.bib. Paper filename = canonical citekey mapping. Transient review streams NOT stored as Markdown (future: render GitHub discussions/issues in-site).
